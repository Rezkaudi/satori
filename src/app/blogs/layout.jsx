
// metadata
export const metadata = {
  title: "Blogs Page",
  description: "Blogs Page",
};

const AboutLayout = ({ children }) => {
  return (
    <main>
      {children}
    </main>
  );
}

export default AboutLayout
